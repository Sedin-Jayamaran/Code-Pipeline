<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Superman • Static Theme Page</title>
  <meta name="description" content="A sleek, single‑file static page themed around Superman." />
  <style>
    :root{
      --blue:#0b3d91;      /* primary */
      --red:#c10016;       /* accent */
      --yellow:#ffd200;    /* highlight */
      --ink:#0a0a0a;       /* text */
      --cloud:#f6f7fb;     /* bg */
      --card:#ffffff;      /* surfaces */
      --muted:#65758b;     /* subtext */
      --ring: rgba(12, 83, 230, .3);
      --radius: 18px;
      --shadow: 0 10px 30px rgba(0,0,0,.12);
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0}
    body{
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji";
      color:var(--ink);
      background: linear-gradient(180deg, var(--cloud) 0%, #eef2ff 40%, #ffffff 100%);
      line-height:1.6;
    }
    a{color:inherit;text-decoration:none}
    .container{max-width:1100px;margin:0 auto;padding:0 20px}
    header{
      position:sticky;top:0;z-index:20;
      backdrop-filter:saturate(160%) blur(8px);
      background:linear-gradient(180deg, rgba(255,255,255,.92), rgba(255,255,255,.7));
      border-bottom:1px solid rgba(12, 18, 46, .08);
    }
    .nav{display:flex;align-items:center;justify-content:space-between;padding:14px 0}
    .brand{display:flex;align-items:center;gap:12px;font-weight:800;letter-spacing:.3px}
    .brand span{font-size:20px}
    .badge{font-size:11px;font-weight:700;background:var(--yellow);color:#111;padding:3px 8px;border-radius:999px;margin-left:6px}

    /* Emblem */
    .emblem{width:38px;height:38px;display:inline-grid;place-items:center;border-radius:10px;
      background: radial-gradient(circle at 30% 30%, #ff4d4d, var(--red) 55%);
      box-shadow:inset 0 -4px 10px rgba(0,0,0,.25), 0 6px 14px rgba(193,0,22,.35);
      border:2px solid #8d0010;
      position:relative;
    }
    .emblem:before, .emblem:after{content:"";position:absolute;inset:6px;border-radius:6px;}
    .emblem:before{border:2px solid var(--yellow);opacity:.85}
    .emblem:after{inset:auto;}
    .s-letter{font-weight:900;font-size:20px;color:var(--yellow);text-shadow:0 1px 0 #8a6b00, 0 2px 8px rgba(0,0,0,.35)}

    /* Hero */
    .hero{padding:80px 0 60px;position:relative;overflow:hidden}
    .hero .bg{
      position:absolute;inset:-80px -40px auto -40px;height:380px;z-index:-1;
      background:
        radial-gradient(60% 60% at 70% 30%, rgba(255,210,0,.18), transparent 60%),
        radial-gradient(60% 60% at 30% 60%, rgba(193,0,22,.15), transparent 65%),
        linear-gradient(135deg, var(--blue), #1a59cc);
      clip-path:polygon(0 0, 100% 0, 100% 70%, 0 100%);
      box-shadow:inset 0 -50px 100px rgba(0,0,0,.12);
    }
    .hero h1{font-size:54px;line-height:1.1;margin:0;color:#fff;text-wrap:balance}
    .hero p{color:#f0f4ff;max-width:720px;margin:16px 0 28px;font-size:18px}
    .cta{display:flex;gap:12px;flex-wrap:wrap}
    .btn{display:inline-flex;align-items:center;gap:10px;font-weight:800;padding:12px 18px;border-radius:999px;border:0;cursor:pointer;transition:transform .12s ease, box-shadow .12s ease}
    .btn:focus{outline:3px solid var(--ring)}
    .btn-primary{background:linear-gradient(180deg, #ffd200, #ffb700);color:#1a1200;box-shadow:0 8px 20px rgba(255,183,0,.35)}
    .btn-primary:hover{transform:translateY(-1px)}
    .btn-ghost{background:rgba(255,255,255,.14);color:#fff;border:1px solid rgba(255,255,255,.4)}

    /* Sections */
    section{padding:56px 0}
    .section-title{font-size:28px;margin:0 0 10px}
    .sub{color:var(--muted);margin:0 0 24px}

    .cards{display:grid;grid-template-columns:repeat(12, 1fr);gap:18px}
    .card{grid-column:span 4;background:var(--card);border-radius:var(--radius);padding:22px;box-shadow:var(--shadow);border:1px solid rgba(12,18,46,.08)}
    .card h3{margin:0 0 6px;font-size:20px}
    .icon{font-size:26px}

    @media (max-width:900px){
      .card{grid-column:span 6}
      .hero h1{font-size:42px}
    }
    @media (max-width:640px){
      .card{grid-column:span 12}
      .hero{padding:64px 0 46px}
      .hero h1{font-size:36px}
    }

    /* Feature band */
    .band{display:grid;grid-template-columns: 1.3fr 1fr;gap:28px;align-items:center}
    .panel{background:var(--card);border-radius:var(--radius);padding:26px;box-shadow:var(--shadow);border:1px solid rgba(12,18,46,.08)}
    .panel p{margin:0;color:#2b3245}

    /* Gallery placeholders */
    .gallery{display:grid;grid-template-columns:repeat(3, 1fr);gap:16px}
    .shot{border-radius:16px;aspect-ratio: 4 / 3;position:relative;overflow:hidden;border:1px solid rgba(12,18,46,.08);box-shadow:var(--shadow)}
    .shot:before{content:"";position:absolute;inset:0;background:
      radial-gradient(60% 90% at 80% 10%, rgba(255,210,0,.25), transparent 40%),
      linear-gradient(135deg, #123a92, #0b3d91 35%, #c10016 90%);
    }
    .shot span{position:absolute;left:12px;bottom:10px;font-weight:700;font-size:14px;color:#fff;text-shadow:0 2px 8px rgba(0,0,0,.5)}

    /* Quote */
    blockquote{margin:0;background:#0b3d91;color:#fff;padding:28px;border-radius:var(--radius);box-shadow:var(--shadow)}
    blockquote p{font-size:18px;margin:0}
    blockquote cite{display:block;margin-top:10px;opacity:.9}

    /* Footer */
    footer{padding:30px 0;border-top:1px dashed rgba(12,18,46,.15);color:#435066}
    .footer-grid{display:flex;gap:18px;justify-content:space-between;flex-wrap:wrap;align-items:center}

    /* Utility */
    .chip{display:inline-flex;align-items:center;gap:8px;padding:6px 10px;border-radius:999px;background:#f1f5ff;border:1px solid rgba(12,18,46,.08);font-weight:700;color:#274585}
    .sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
  </style>
</head>
<body>
  <!-- Header -->
  <header aria-label="Site header">
    <div class="container nav">
      <a class="brand" href="#top">
        <span class="emblem" aria-hidden="true"><span class="s-letter">S</span></span>
        <span>Superman</span>
        <span class="badge" title="Fan Made">FAN</span>
      </a>
      <nav aria-label="Primary">
        <a href="#about">About</a>
        &nbsp;&nbsp;•&nbsp;&nbsp;
        <a href="#powers">Powers</a>
        &nbsp;&nbsp;•&nbsp;&nbsp;
        <a href="#gallery">Gallery</a>
        &nbsp;&nbsp;•&nbsp;&nbsp;
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <!-- Hero -->
  <main id="top">
    <section class="hero" aria-label="Hero">
      <div class="bg" aria-hidden="true"></div>
      <div class="container">
        <div class="chip" aria-hidden="true">
          <span>🦸‍♂️</span><span>Truth • Justice • Hope</span>
        </div>
        <h1>Up, up, and away!</h1>
        <p>
          A clean, single-file static page celebrating the iconic Man of Steel. Built with accessible, responsive HTML + CSS only — easy to customize, royalty‑free graphics, and zero dependencies.
        </p>
        <div class="cta">
          <a class="btn btn-primary" href="#powers">See Powers</a>
          <a class="btn btn-ghost" href="#about">Learn More</a>
        </div>
      </div>
    </section>

    <!-- About -->
    <section id="about" aria-labelledby="about-title">
      <div class="container">
        <h2 class="section-title" id="about-title">About Superman</h2>
        <p class="sub">From Krypton to Metropolis — a timeless story of hope, courage, and the will to help.</p>
        <div class="band">
          <div class="panel">
            <p>
              Sent from the doomed planet Krypton and raised as Clark Kent, Superman stands as a symbol of hope. With a strong moral compass and a heart for humanity, he uses incredible powers to protect the innocent and inspire the world.
            </p>
          </div>
          <div class="panel" aria-hidden="true">
            <svg viewBox="0 0 240 160" width="100%" height="100%" role="img" aria-label="Stylized skyline of Metropolis">
              <defs>
                <linearGradient id="g1" x1="0" y1="0" x2="1" y2="1">
                  <stop offset="0%" stop-color="#123a92"/>
                  <stop offset="100%" stop-color="#0b3d91"/>
                </linearGradient>
              </defs>
              <rect width="240" height="160" fill="url(#g1)"/>
              <g fill="#ffd200" opacity="0.14">
                <circle cx="190" cy="40" r="20"/>
              </g>
              <g fill="#fff" opacity=".1">
                <rect x="20" y="60" width="30" height="80"/>
                <rect x="60" y="40" width="26" height="100"/>
                <rect x="92" y="70" width="22" height="70"/>
                <rect x="120" y="50" width="28" height="90"/>
                <rect x="156" y="65" width="20" height="75"/>
                <rect x="182" y="55" width="22" height="85"/>
              </g>
              <g>
                <path d="M30,30 l40,-20 l40,20 l-40,80 z" fill="#c10016" stroke="#8d0010" stroke-width="2"/>
                <text x="70" y="70" font-family="ui-sans-serif, system-ui" font-size="38" font-weight="900" fill="#ffd200" text-anchor="middle">S</text>
              </g>
            </svg>
          </div>
        </div>
      </div>
    </section>

    <!-- Powers -->
    <section id="powers" aria-labelledby="powers-title">
      <div class="container">
        <h2 class="section-title" id="powers-title">Signature Powers</h2>
        <p class="sub">A few of the abilities that make the Man of Steel a living legend.</p>
        <div class="cards" role="list">
          <article class="card" role="listitem"><div class="icon">💪</div><h3>Super Strength</h3><p>Lift, move, and withstand forces beyond imagination.</p></article>
          <article class="card" role="listitem"><div class="icon">🛡️</div><h3>Invulnerability</h3><p>Bullets, explosions, and extremes — no problem.</p></article>
          <article class="card" role="listitem"><div class="icon">🛩️</div><h3>Flight</h3><p>Defies gravity with effortless control and speed.</p></article>
          <article class="card" role="listitem"><div class="icon">⚡</div><h3>Super Speed</h3><p>Faster than a speeding bullet — reaction and travel.</p></article>
          <article class="card" role="listitem"><div class="icon">🔥</div><h3>Heat Vision</h3><p>Concentrated beams capable of precision or power.</p></article>
          <article class="card" role="listitem"><div class="icon">🧊</div><h3>Freeze Breath</h3><p>Super‑cooled gusts to stop fires or stabilize threats.</p></article>
          <article class="card" role="listitem"><div class="icon">👂</div><h3>Super Hearing</h3><p>Attunes to distant cries for help across the city.</p></article>
          <article class="card" role="listitem"><div class="icon">🔍</div><h3>X‑Ray Vision</h3><p>Sees through obstacles (ethically!) when lives depend on it.</p></article>
          <article class="card" role="listitem"><div class="icon">☀️</div><h3>Solar Empowerment</h3><p>Recharges under Earth’s yellow sun; hope made literal.</p></article>
        </div>
      </div>
    </section>

    <!-- Gallery -->
    <section id="gallery" aria-labelledby="gallery-title">
      <div class="container">
        <h2 class="section-title" id="gallery-title">Gallery</h2>
        <p class="sub">Drop in your own artwork or photos — these are styled placeholders.</p>
        <div class="gallery">
          <div class="shot"><span>Metropolis Skyline</span></div>
          <div class="shot"><span>Above the Clouds</span></div>
          <div class="shot"><span>Fortress of Solitude</span></div>
        </div>
      </div>
    </section>

    <!-- Quote -->
    <section aria-label="Quote">
      <div class="container">
        <blockquote>
          <p>“There is a right and a wrong in the universe — and the distinction is not hard to make.”</p>
          <cite>— Superman</cite>
        </blockquote>
      </div>
    </section>

    <!-- Contact / CTA -->
    <section id="contact" aria-labelledby="contact-title">
      <div class="container band">
        <div>
          <h2 class="section-title" id="contact-title">Build Your Own Hero Page</h2>
          <p class="sub">Copy this file, swap colors, and replace text/images to personalize.</p>
          <div class="cta" style="margin-top:10px">
            <a class="btn btn-primary" href="#top">Back to Top</a>
            <a class="btn btn-ghost" href="#gallery">Customize Gallery</a>
          </div>
        </div>
        <form class="panel" aria-label="Quick contact form" onsubmit="event.preventDefault(); alert('Thanks! This demo is static.');">
          <label class="sr-only" for="name">Name</label>
          <input id="name" name="name" placeholder="Your name" style="width:100%;padding:12px 14px;border-radius:12px;border:1px solid #ccd5e0;margin-bottom:12px" />
          <label class="sr-only" for="email">Email</label>
          <input id="email" name="email" placeholder="Email (optional)" style="width:100%;padding:12px 14px;border-radius:12px;border:1px solid #ccd5e0;margin-bottom:12px" />
          <label class="sr-only" for="msg">Message</label>
          <textarea id="msg" name="message" rows="4" placeholder="Say hello, Kryptonian style…" style="width:100%;padding:12px 14px;border-radius:12px;border:1px solid #ccd5e0"></textarea>
          <div class="cta" style="margin-top:12px">
            <button class="btn btn-primary" type="submit">Send</button>
          </div>
        </form>
      </div>
    </section>
  </main>

  <footer role="contentinfo">
    <div class="container footer-grid">
      <small>Fan tribute. Not affiliated with DC Comics or Warner Bros.</small>
      <small>Made with ❤️ for the Man of Steel.</small>
    </div>
  </footer>

  <script>
    // Smooth scroll for internal links
    document.querySelectorAll('a[href^="#"]').forEach(a=>{
      a.addEventListener('click', e=>{
        const id = a.getAttribute('href');
        const el = document.querySelector(id);
        if(el){ e.preventDefault(); el.scrollIntoView({behavior:'smooth', block:'start'}); }
      });
    });
  </script>
</body>
</html>
