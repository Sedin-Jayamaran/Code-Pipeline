<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Simple HTML Text</title>
</head>
<body>
  <h1>Hello — Simple HTML Text</h1>
  <p>This is a minimal HTML page containing simple text. Paste this into a file named <code>index.html</code> and open it in your browser.</p>

  <p><strong>Notes:</strong></p>
  <ul>
    <li>Use this as a starting point for static pages.</li>
    <li>To serve it locally: <code>python -m http.server 8000</code> (in that directory) and browse <code>http://localhost:8000</code>.</li>
  </ul>
</body>
</html>
